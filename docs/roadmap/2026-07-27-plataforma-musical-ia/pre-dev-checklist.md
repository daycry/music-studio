---
titulo: Checklist de preparación pre-desarrollo — Plataforma musical IA
slug: plataforma-musical-ia
fecha: 2026-09-01
actualizado: 2026-09-02
autor: revision-2026-09-01
estado: abierto
spec: ./spec.md
plan: ./improvement-plan.md
tasks: ./tasks.md
---

# Checklist de preparación pre-desarrollo

> Barrido exhaustivo de **cabos sueltos** en toda la planificación (spec, evaluación, plan, tasks, test-plan, brief, gates, config), hecho el 2026-09-01 tras la revisión de coherencia del mismo día. Cada ítem cita su fuente. Objetivo: que nada de esto sorprenda una vez arrancado el desarrollo. **55 ítems** (+1 guardarraíl operativo). *(Actualizado el 2026-09-01 con CS-49, la ratificación económica del instalador `T-86`/D-30, y con **CS-50 y CS-51**, los dos bloqueos descubiertos al cerrar los ítems 5–7. Actualizado el **2026-09-02** con la **sección D — CS-52 a CS-55**, los cuatro cabos sueltos que aparecieron al ejecutar F2 de verdad.)*
>
> **🆕 Adaptación a proyecto personal (2026-09-01 — decisión del propietario: proyecto personal en solitario):** de los 9 ítems que bloqueaban el arranque real de F2, **4 se cierran** por la adaptación a modo solo (CS-05/06/07/08 → `gates/gobernanza.md`, con GC-01 conservando la versión corporativa), **2 pasan a opcionales** (CS-38/CS-39, solo si se quiere medir/usar el modo cloud) y **quedan 3 bloqueos reales: los ítems 5–7 (máquina GPU local, toolkit, pesos)**. Los ítems legales de la sección B cambian su fecha límite al **gate de comercialización GC-01** (`gates/gobernanza.md` §8).
>
> **🆕 Actualización 2026-09-01 (tarde, `/dev-cycle`):** **los 3 bloqueos originales (ítems 5, 6 y 7) están CERRADOS con evidencia ejecutada** — GPU confirmada, toolkit verificado, pesos descargados y atestiguados. Pero al cerrarlos aparecieron **dos bloqueos nuevos que la planificación no contemplaba**, registrados como ítems **7-bis (CS-50)** y **7-ter (CS-51)**: el fichero único de pesos que espera el adapter **no existe en ningún repositorio público** (hay que construirlo), y el suelo de VRAM codificado en `_timing.py` **rechaza esta máquina por 1 MiB**. Ninguno es de entorno: el primero es trabajo de `T-03`/`T-05`, el segundo exige una **decisión sobre un umbral de la spec**. La lección: los ítems 5–7 no eran «una tarde de trabajo del propietario» sino la puerta de entrada a dos supuestos falsos.
>
> 🆕 **Actualización 2026-09-02 (cierre de F2):** el aviso de arriba —«F2 NO puede arrancar todavía»— **ha quedado superado**. **CS-50 se cerró** (el artefacto se construyó, y después se reconstruyó con planificador: 7.529.590.739 B, 1.492 tensores) y **CS-51 sigue formalmente abierto** solo por la firma de la decisión, no por el código: el arreglo lleva todo el día en uso real. F2 no solo arrancó — **`T-05`, `T-06` y `T-07` están cerradas** y el pipeline genera audio de 240 s en castellano. Lo que quedó fuera fue lo que necesita **RunPod, aparcado por presupuesto el 2026-09-02**: el arranque en frío de S-01 (`T-03`) y la concurrencia en L40S (`T-04`). Y ejecutar el spike destapó **cuatro cabos sueltos nuevos**, en la nueva **sección D**. La lección se repite: cada bloqueo cerrado con las manos en el código reveló supuestos que la planificación daba por buenos.
>
> Regla de uso: marcar el checkbox **solo con la evidencia hecha** (nombramiento por escrito, documento archivado, comando ejecutado). Este fichero no sustituye al ledger (`tasks.md`); lo complementa con lo que el ledger no modela (decisiones externas, entorno, firmas).

---

## A. Bloquean el arranque real del desarrollo (F2 — spikes) · en orden de cierre recomendado

| # | ID | Cabo suelto | Fuente | Dueño | Acción de cierre |
|---|----|-------------|--------|-------|------------------|
| ☑ 1 | CS-05 | ~~**Faltan 2 de 3 evaluadores de G1**~~ — **cerrado 2026-09-01**: cerrado por adaptación a proyecto personal — ver `gates/gobernanza.md` §2 (evaluador único = propietario, protocolo numérico íntegro, riesgo aceptado; **GC-01 conserva la versión corporativa para el futuro comercial**). | `gates/gobernanza.md` §2 · `tasks.md` T-02 | Propietario | ✅ Hecho: `T-02` completado en modo solo |
| ☑ 2 | CS-06 | ~~**3–5 usuarios piloto sin nombrar**~~ — **cerrado 2026-09-01**: cerrado por adaptación a proyecto personal — ver `gates/gobernanza.md` §3 (usuario piloto = propietario; G3 reinterpretado en modo solo sin degradar su función; **GC-01 conserva la versión corporativa para el futuro comercial**). | `spec.md` I-20 · `gates/gobernanza.md` §3 | Propietario | ✅ Hecho: `T-02` completado en modo solo |
| ☑ 3 | CS-07 | ~~**Decisión construir-vs-comprar sin documentar**~~ — **cerrado 2026-09-01**: cerrado por adaptación a proyecto personal — documentada en `gates/gobernanza.md` §4 («construir»: aprendizaje, control total, self-hosting). | `gates/gobernanza.md` §4 · `tasks.md` T-02 | Propietario | ✅ Hecho |
| ☑ 4 | CS-08 | ~~**Dos ofertas con indemnización comercial no solicitadas ni archivadas**~~ — **cerrado 2026-09-01**: cerrado por adaptación a proyecto personal — sin sentido en ámbito personal; **la comparativa comprar se reevalúa en GC-01** (`gates/gobernanza.md` §4 y §8g). | `evaluation.md` §6.5b, R-19 · `gates/gobernanza.md` §4 | Propietario | ✅ Diferido a GC-01 |
| ☑ 5 | CS-34 | **VRAM real de la máquina local objetivo desconocida (I-21)** — sin máquina confirmada no hay dónde correr `T-03`/`T-05`/`T-09` en modo GPU local, ni se sabe si el offloading aplica ni si S-02/S-02b valen tal cual. **✅ CERRADO 2026-09-01: NVIDIA GeForce GTX 1070, 8 GB de VRAM** — justo en el suelo de D-06/D-29: viable con **offloading obligatorio**. Matiz: Pascal (sin BF16/tensor cores) → tiempos por pista muy por encima de la referencia S-02; `T-03` medirá la cifra real, y si lo local resulta impracticable, RunPod (ítems 8–9) es la alternativa de medición. Despliegue final: esta máquina u otra (instalador T-86). | `spec.md` I-21 · `apps/runner/spikes/README.md` §2 | **Propietario** *(dueño asignado 2026-09-01, modo solo)* | Confirmar máquina, modelo de GPU y VRAM |
| ☑ 6 | CS-35 | ~~**NVIDIA Container Toolkit sin verificar** en esa máquina (`docker run --rm --gpus all nvidia/cuda…` nunca ejecutado)~~ — **✅ CERRADO 2026-09-01: el toolkit funciona, la GPU es visible dentro del contenedor.** Evidencia ejecutada en la máquina de referencia: (1) driver **582.66** (rama 580, última para Pascal), `nvidia-smi` en host lista `NVIDIA GeForce GTX 1070`, **8192 MiB**, CUDA 13.0; (2) **Docker Engine 29.7.2**, servidor `linux` sobre backend **WSL2** (distro por defecto Ubuntu, versión 2); (3) `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi` → **lista la GTX 1070 con 8192 MiB dentro del contenedor** (NVIDIA-SMI 580.159.06 / Driver 582.66 / CUDA 13.0). Sigue vigente la nota de que el preflight de `T-86` (D-30, **pendiente de ratificación**) automatizará esta comprobación en **cada máquina destino**; esta verificación manual cubre solo la **máquina de referencia de los spikes**. | `apps/runner/spikes/README.md` §2 | **Propietario** *(2026-09-01)* | ✅ Hecho — los 3 pasos verificados el 2026-09-01 |
| ☑ 7 | CS-36 | ~~**Pesos `safetensors` de ACE-Step 1.5 no confirmados** como disponibles/montados en ninguna ruta real~~ — **✅ CERRADO 2026-09-01: descargados, verificados y con procedencia registrada.** Origen **`ACE-Step/Ace-Step1.5` fijado a la revisión `19671f406d603126926c1b7e2adc169acbcade22`** (nunca `main`), **17 ficheros / 6,333 GB**, **17/17 con hash verificado contra la atestación del publicador** (LFS → SHA-256; no-LFS → git blob SHA-1), **0 fallos, 0 sin atestación**. Ruta: `D:\srv\ace-step\upstream\<rev>\`. SHA-256 de los tres pesos: DiT `3f6e0797…c6e422d7` (4,788 GB) · text encoder Qwen3-Embedding-0.6B `0437e45c…d48e23fd` (1,192 GB) · VAE `da17edb6…ed9a99b0` (0,337 GB). Manifiesto en `D:\srv\ace-step\provenance\MANIFEST.json` con licencias, `training_data_declaration` y advertencias. El pickle obligatorio `silence_latent.pt` (`a778e9dd…49ad6358b`) queda **en cuarentena (`D:\srv\ace-step\quarantine\`), fuera del volumen montado y sin cargar jamás** (D-14). **No se descargó** el LM `acestep-5Hz-lm-1.7B` (3,709 GB, planificador CoT que no genera audio) ni ninguna variante XL (no caben en 8 GiB). **`D:\srv\ace-step\weights\` contiene ya el artefacto fusionado** `ace_step_1_5.safetensors` (construido más tarde el mismo día — ver el ítem 7-bis); estuvo vacío a propósito hasta entonces, porque ese fichero no existe publicado y hubo que construirlo. | `apps/runner/spikes/README.md` §2 · `D:\srv\ace-step\provenance\MANIFEST.json` | **Propietario** *(2026-09-01)* | ✅ Hecho — descargado, verificado y atestiguado el 2026-09-01 |
| ☑ **7-bis** | **CS-50** 🆕 | **✅ CERRADO 2026-09-01: el artefacto se ha construido y verificado.** `apps/runner/tools/build_artifact.py` fusiona por streaming los cuatro componentes de la revisión fijada en `D:\srv\ace-step\weights\ace_step_1_5.safetensors` — **6.163.551.450 B, 1.177 tensores** (`dit.` 677 + `text_encoder.` 310 + `vae.decoder.` 182 + `aux.` 8), SHA-256 **`3faa5ac9caeb06b5778aa86ac7246c16d560d73dffff50b94a097bf15812d947`** (exportable como `ACE_STEP_WEIGHTS_SHA256`). BF16→FP16 offline con **0 desbordamientos** y 2.181 flush-to-zero sobre 3.074.063.112 elementos; configs y `tokenizer.json` empotrados como tensores U8 (porque `load_file()` descarta `__metadata__` y la factoría no recibe `weights_dir`), de modo que **un solo hash sella pesos, configs, tokenizer y latente**; el latente de silencio convertido **sin ejecutar su pickle**, con rechazo por lista blanca de *opcodes* (no de GLOBALs: `INST`/`OBJ`/`EXT1-4` referencian callables sin emitir `GLOBAL`). `--selftest` y `--verify` en verde. *(Texto original del cabo suelto, conservado para trazabilidad:)* ~~**El fichero único que espera el adapter NO EXISTE en ningún repositorio público.**~~ `adapter.py` hace `load_file(<un solo fichero>)` → `factoria(state_dict=…)` y su `DEFAULT_WEIGHTS_FILE` es `ace_step_1_5.safetensors`; upstream distribuye el modelo en **cuatro componentes separados** (DiT, text encoder, VAE, silence latent) y **ningún artefacto publicado satisface ese contrato**. El único candidato de fichero único, el *all-in-one* de `Comfy-Org` (10,025 GB), quedó **descartado** por tres motivos verificados: (a) **no contiene el silence latent** (se buscó en sus 1.664 claves: ningún tensor con shape `[1,64,15000]`) y el código upstream aborta sin él en el camino *text2music*; (b) **no trae tokenizers ni configs**, así que obligaría a citar igualmente al upstream — dos orígenes en vez de uno; (c) `load_file(ruta, device='cuda:0')` metería **9,337 GiB en una tarjeta de 8 GiB** → OOM determinista *antes* de que corra la factoría. **Consecuencia:** el artefacto final hay que **construirlo localmente** fusionando los componentes verificados, y el volumen `weights/` no puede poblarse hasta entonces. **Es trabajo de `T-03`/`T-05`, no una precondición de entorno** — se registra aquí porque invalida el supuesto implícito de que «descargar los pesos» bastaba para arrancar F2. | `apps/runner/adapters/ace_step/adapter.py:602-664` · `apps/runner/spikes/README.md` §2 | Quien ejecute `T-03`/`T-05` | Diseñar el fusor por streaming (sin reinterpretar bytes) y el canal para configs/tokenizer, que el contrato actual de la factoría **no ofrece** |
| ☐ **7-ter** | **CS-51** 🆕 | ⚙️ **Arreglo implementado y verificado el 2026-09-01, PERO pendiente de tu ratificación** (por eso sigue abierto). **Actualización 2026-09-02:** el arreglo ha sostenido **toda la jornada de mediciones de F2** — cada informe de generación registra `8191 MB de VRAM: por encima del suelo efectivo de 8128 MB (nominal 8192 MB, D-06)…`, y `gpu_tiers.py` reutiliza la misma regla al calcular el nivel sobre el nominal redondeado. **Lo único que falta sigue siendo la firma**: registrar la decisión como **D-xx**, por tocar un umbral citado en la spec. Solución elegida: **no se toca `VRAM_FLOOR_MB = 8192`** —el número que cita la spec— sino que se añade `VRAM_FLOOR_TOLERANCE_MB = 64` y se compara `vram_total_mb + tolerancia < suelo`. El umbral **declarado** queda intacto y el **efectivo** pasa a 8128 MB, que cubre la reserva del driver sin dejar pasar tarjetas realmente inferiores. 13 tests nuevos (`apps/runner/tests/test_vram_floor.py`), incluido el caso frontera 8191 y un 6144 que debe seguir abortando; suite completa en verde (85 tests). Confirmado **dentro del contenedor**: `total_memory = 8.589.672.448 B = 8191 MiB`. **Falta:** registrar la decisión como **D-xx** con tu ratificación, por tocar un umbral citado en `spec.md` §11.1 (D-06) y D-29. *(Texto original, conservado:)* ~~🚨 **BLOQUEANTE VERIFICADO: el código del proyecto rechaza esta máquina por 1 MiB.**~~ `apps/runner/spikes/_timing.py:102` fija `VRAM_FLOOR_MB = 8 * 1024` (**8192**) y las líneas 307/328 leen la VRAM como `int(torch.cuda.get_device_properties(0).total_memory) // (1024*1024)`. Medido hoy en la máquina de referencia contra `nvcuda.dll`: `cuDeviceTotalMem_v2 = 8.589.672.448 B = 8191,75 MiB` → **8191**. Por tanto `decide_offloading()` (línea 362, `if vram_total_mb < VRAM_FLOOR_MB`) devuelve **`NO VIABLE`** y `adapter._load_sync()` aborta **antes de tocar los pesos**. Los 8192 MiB de `nvidia-smi` son la cifra NVML nominal; CUDA descuenta la reserva del driver (`Reserved: 132 MiB`). **Ninguna GPU de 8 GB pasa este suelo**, así que el «suelo de 8 GB» de D-06/D-29 es inalcanzable tal y como está codificado. Requiere **decisión explícita** (p. ej. bajar el suelo a 7800 MB y medir con `torch.cuda.mem_get_info()`), no un parche silencioso: toca un umbral de la spec. | `apps/runner/spikes/_timing.py:102,307,328,362` · `spec.md` §11.1 (D-06) · `improvement-plan.md` (D-29) | **Propietario** (decisión) + quien ejecute `T-03` | Decidir el nuevo suelo y su justificación, y registrarlo como decisión (D-xx) antes de la primera corrida real |
| ☐ 8 *(opcional)* | CS-38 | **Cuenta/pod real de RunPod sin aprovisionar** para medir el arranque en frío (S-01/S-01b). **🆕 2026-09-01 — pasa a OPCIONAL**: solo necesario si quieres medir/usar el modo cloud; para uso personal en GPU local puedes diferirlo — la medición de arranque en frío S-01/S-01b quedaría pendiente y el modelo de coste cloud de `evaluation.md` §6 como informativo. **🆕 2026-09-02 — diferido, con consecuencias ahora concretas y contadas:** el propietario **aparca RunPod por presupuesto**. Esto **no bloquea G1** (que es escucha de calidad, no capacidad) pero deja **tres cosas medidas a medias, en tres tareas distintas**: (a) el arranque en frío de S-01 en `T-03` — lo medido en local es solo el término de carga del modelo, sin *scheduling*, *pull* ni descarga de pesos (ver **CS-55**); (b) la concurrencia en L40S en `T-04`, de la que depende `T-39` — y con ella el **ahorro de ~128 €/mes que no debe darse por ganado** en `evaluation.md` §6.3/§12.1; (c) la ejecución de **`T-29`/`T-34` (G1-bis)**, que el spike `T-06` demostró que **no caben en la GPU local** (HeartMuLa: 15,8 GB de pesos más HeartCodec en F32). Ninguna de las tres se cierra sin GPU de ≥ 24 GB. | `apps/runner/spikes/README.md` §7 · `spec.md` confirmación 13 | Propietario | ⏸️ **Aparcado por decisión del propietario (2026-09-02)** — (opcional) dar de alta cuenta y pod L40S; credencial nunca en el repo |
| ☐ 9 *(opcional)* | CS-39 | **Sin tope de gasto € agregado** para la sesión de medición contra RunPod real (solo hay `max_gpu_seconds` por trabajo; el kill switch agregado `T-41` llega en F6). **🆕 2026-09-01 — pasa a OPCIONAL**: solo necesario si se ejecuta la medición cloud del ítem 8. **🆕 2026-09-02 — sin efecto práctico mientras RunPod siga aparcado** (ítem 8), pero **no se cierra**: es precondición de la primera sesión cloud que se ejecute, y esa sesión es justo la que arrastra tres tareas a medio medir. | `apps/runner/spikes/README.md` §9 (D-17) | Propietario | (Opcional) Fijar un tope en € por sesión de spike |

> **Guardarraíl operativo (nuevo, 2026-09-01) — G-01:** toda corrida de **medición real** (spikes `T-03`/`T-04`/`T-09` y generación de pistas de G1) se lanza con **`ACE_STEP_REQUIRE_GPU=1`**, para que un entorno mal configurado (torch sin CUDA, contenedor sin `--gpus all`) **falle en el arranque** en vez de degradar al mock. Sin la variable, el runner cae al mock con el informe etiquetado como simulado (comportamiento corregido en la revisión 2026-09-01); con ella, se niega a arrancar y explica qué comprobar. Añadirla a los comandos de lanzamiento documentados en `spikes/README.md` cuando se ejecute F2.

*(Nota: el `Dockerfile` de `T-05`, `concurrency_profile.py` de `T-04` y `ace_step_shim.py` de `T-03` no aparecen aquí — no son precondiciones sino los propios entregables de esas tareas, ya en el ledger.)*

---

## B. Con fecha límite natural en un gate o fase

| # | ID | Cabo suelto | Fuente | Fecha límite natural | Dueño |
|---|----|-------------|--------|----------------------|-------|
| ☑ 10 | CS-10 | ~~Disponibilidad de agenda del supervisor musical para G1/G1-bis sin fechas comprometidas~~ — **cerrado 2026-09-01**: el propietario evalúa, sin agenda de terceros que comprometer (modo solo, `gates/gobernanza.md` §2.1). | `gates/gobernanza.md` §2.1 | — | Propietario |
| ☐ 11 | CS-03 | **ToS de Suno sin verificar** — el protocolo de G1 usa su salida como línea base ciega (S-11). En ámbito personal, responsabilidad del propietario (`gates/gobernanza.md` §2.2, nota) | `spec.md` S-11 · `tasks.md` T-08 | **Gate de comercialización GC-01** (antes de cualquier uso comercial o con terceros) — `gates/gobernanza.md` §8c | Propietario (consulta legal real en GC-01) |
| ☐ 12 | CS-41 | **Esquema del manifiesto v1 sin fecha de firma con legal** (G2 solo reservó la agenda; riesgo RP-03) | `tasks.md` T-26 · `gates/g2-matriz-resultados.md` §5.2 | Antes de **iniciar F5** (`T-26`) — en modo personal la firma formal de legal se difiere a **GC-01** (el propietario valida el esquema); imprescindible antes de uso comercial | Propietario (consulta legal real en GC-01) |
| ☑ 13 | CS-46 | ~~**Flujo de ramas/merge sin decidir**~~ — **cerrado 2026-09-01**: decisión documentada en `CLAUDE.md` (flujo solo: ramas `feature/<slug>` y **merge directo a `main` sin PR**; commits `T-XX`). | `CLAUDE.md` convenciones | — | Propietario |
| ☐ 14 | CS-01 | **Informe escrito de legal (I-05) sin archivar**; la respuesta de G2 fue informal | `tasks.md` T-01 (deuda declarada) | **Gate de comercialización GC-01** (antes de cualquier uso comercial o con terceros) — `gates/gobernanza.md` §8a | Propietario |
| ☐ 15 | CS-04 | **Anexo A («verdad incómoda», §10.7) sin firmar** — mitigación declarada de R-01; en modo solo la firma es la **aceptación consciente propia** del propietario | `gates/g2-matriz-resultados.md` §8.3/§10 | **Gate de comercialización GC-01** (antes de cualquier uso comercial o con terceros) — `gates/gobernanza.md` §8f | Propietario |
| ☐ 16 | CS-02 | **I-05b (protegibilidad/exclusividad del output) sin respuesta** | `spec.md` I-05b · `evaluation.md` R-29 | **Gate de comercialización GC-01** (antes de cualquier uso comercial o con terceros; imprescindible antes de prometer exclusividad) — `gates/gobernanza.md` §8b | Consulta legal real (GC-01) |
| ☐ 17 | CS-18 | **I-10** — ¿política corporativa que obligue al watermarking? | `spec.md` I-10 | Antes de **priorizar/aprobar Fase 2** — en modo personal no hay política corporativa: lo decide el propietario | Propietario |
| ☐ 18 | CS-21 | **I-13** — robustez de SilentCipher/AudioSeal sobre música transcodificada (prueba en `T-57`) | `spec.md` I-13 · `evaluation.md` R-04 | Antes de **ejecutar Fase 2** (C-10b) | Quien ejecute T-57 |
| ☐ 19 | CS-22 | **I-13b** — fichas de licencia del pipeline: Demucs confirmado **CC-BY-NC** (bloquea C-06 tal cual: alternativa con pesos MIT o licenciar); para Fase 1, la verificación de ffmpeg/HeartCodec/HeartTranscriptor vive en `T-33` | `spec.md` I-13b · `CLAUDE.md` invariantes | Demucs: antes de **Fase 2**; resto: en **F5** (`T-33`) | Quien ejecute T-33 |
| ☑ 20 | CS-25 | ~~**I-16** — propietario operativo tras la entrega sin nombrar~~ — **cerrado 2026-09-01**: propietario operativo = el propietario (I-16 cerrada, `gates/gobernanza.md` §6.1); el OPEX es su tiempo, cifra informativa. Se revisita en GC-01 §8g si deja de ser él. | `spec.md` I-16 · `evaluation.md` R-16 | — | Propietario |
| ☑ 21 | CS-11 + CS-27 | ~~**116 h de no-desarrollo sin reserva formal**, y sin tarifa interna (I-18)~~ — **cerrado 2026-09-01**: **N/A en modo personal** (I-18 cerrada) — todo ese trabajo es tiempo del propietario, sin tarifa que imputar (`gates/gobernanza.md` §6.2). Las horas de legal renacen como consulta real de pago en GC-01 §8a/§8b; el DPO solo en el escenario §8e (Fase 4, no-go). | `evaluation.md` §6.5d, R-18 · `gates/gobernanza.md` §6.2 | — | Propietario |
| ☐ 22 | CS-40 | **Cuenta/pod de producción RunPod (opción G, ≈ 128 €/mes) sin alta confirmada** | `evaluation.md` §6.3 · `improvement-plan.md` §4.1 | Antes de **F6/F7** (activar producción) | Propietario operativo (→ CS-20) |
| ☐ 23 | CS-47 | **Tamaño máximo de audio de entrada para C-08**: `⚠️ definir en C-08` | `spec.md` §12.1 | Antes de **Fase 3** (F11) | Quien planifique F11 |
| ☐ 24 | CS-19 | **I-11** — presupuesto máximo total aprobado (¿hay techo por encima de los 39.360 €?). **🆕 2026-09-01:** el presupuesto **lo decide el propietario**; las cifras en € son informativas en modo personal (`spec.md` I-11) | `spec.md` I-11 | Antes de **aprobar Fase 2/3** | Propietario |
| ☐ 25 | CS-49 | **Delta de `T-86` sin ratificar** — el instalador del runner GPU local (D-30: preflight automatizado, pesos con SHA-256, config con `ACE_STEP_REQUIRE_GPU=1`, desinstalación limpia) está estimado en **+32 h base / +38,4 h con margen / +1.920 €** (rango 24–40 h) y **propuesto, no autorizado**. **Ratificar el delta de `T-86` (+32 h / +1.920 €) antes de ejecutarla**; sin ratificación no pasa a `en-progreso` aunque `T-05`/`T-36`/`T-85` estén cerradas. Cerrar antes **I-22** (SO objetivo) reduce la dispersión del rango. | `spec.md` D-30, I-22 · `tasks.md` T-86 · `improvement-plan.md` §5 (F6) | Antes de **ejecutar `T-86`** (F6) | Propietario (ratificación) |

---

## C. Higiene y decisiones sin fecha dura (cerrar cuando toque; ninguna impide avanzar hoy)

| # | ID | Cabo suelto | Fuente | Nota |
|---|----|-------------|--------|------|
| ☑ 26 | CS-12/CS-29/CS-31 | ~~**I-01 — equipo asignado / perfil combinado (S-04)**~~ — **cerrado 2026-09-01**: I-01 cerrada — el equipo es **el propietario en solitario, con la IA como multiplicador** (`spec.md` §10). Consecuencia asumida: **el calendario de 25–28 semanas pasa a ser orientativo**, no compromiso | `spec.md` I-01, S-04 · `improvement-plan.md` §1 | ✅ Cerrado por adaptación a proyecto personal |
| ☑ 27 | CS-09 | ~~Acumulación de roles (supervisor musical + responsable) registrada como riesgo **no validado por dirección**~~ — **cerrado 2026-09-01**: superado por la adaptación a proyecto personal — en modo solo la acumulación es inevitable y queda **aceptada por escrito** en `gates/gobernanza.md` §2.1 (con la opción recomendada de 1–2 oyentes externos informales); evaluadores independientes se revisitan en GC-01 §8g | `gates/gobernanza.md` §2.1 | ✅ Riesgo aceptado por escrito |
| ☑ 28 | CS-42 | ~~**RACI de gates futuros sin nombres**~~ — **cerrado 2026-09-01**: **todos los papeles = el propietario** (`gates/gobernanza.md` §5); umbrales y stop-loss intactos como autodisciplina. Se revisita en GC-01 §8g si se comercializa | `evaluation.md` §10.1 · `gates/gobernanza.md` §5 | ✅ Cerrado por adaptación a proyecto personal |
| ☐ 29 | CS-13 | I-02 — ¿GPUs propias/cloud corporativo disponibles? | `spec.md` I-02 | Podría abaratar la infraestructura |
| ☐ 30 | CS-16 | I-08 — precio real de storage/egress | `spec.md` I-08 | Precisión del coste a 24 meses |
| ☐ 31 | CS-17 | I-09 — latencia máxima aceptable por los usuarios | `spec.md` I-09 | Valida el gasto del pod caliente (≈ 83 €/mes) |
| ☐ 32 | CS-20 | I-12 — idiomas requeridos para el canto | `spec.md` I-12 | Afecta a la selección/validación de modelos |
| ☐ 33 | CS-23 | I-14 — precio de GPU cloud de 24 GB (podría reducir a la mitad el coste GPU) | `spec.md` I-14 | Revisar al montar F6 |
| ☐ 34 | CS-24 | I-15 — plan de entornos dev/stage/prod; coste no-GPU fuera del presupuesto ratificado | `spec.md` I-15 | Decidir antes de desplegar stage |
| ☐ 35 | CS-26 | I-17 — política de retención de logs/datos personales (observabilidad `T-24`, RGPD) | `spec.md` I-17 | Decidir antes de F5 idealmente |
| ☐ 36 | CS-28 | I-19 — idiomas de UI adicionales al castellano | `spec.md` I-19 | Alcance de Fase 2 |
| ☐ 37 | CS-15 | I-04 — integración MAM (fuera de alcance declarado, S-07) | `spec.md` I-04 | Solo si cambia el alcance |
| ☐ 38 | CS-14 | I-03 — catálogo musical licenciado (solo afecta a la Fase 4, ya en no-go) | `spec.md` I-03 | Sin acción salvo revisión del no-go |
| ☑ 39 | CS-43 | ~~Precios de tokens sin reverificar desde 2026-07-27~~ — **cerrado 2026-09-01**: reverificados contra la doc oficial, Opus 5 sigue a 5/25 $/M; `rates.json` actualizado | `.claude/rates.json` | Cadencia sugerida: reverificar al arrancar cada fase |
| ☐ 40 | CS-44 | Opt-in de Confluence no activado (`.claude/confluence.json` no existe) | `CLAUDE.md` | Decidir con `/setup` si se quiere publicar la doc |
| ☐ 41 | CS-45 | Opt-in de Jira no activado (`.claude/jira.json` no existe) | `CLAUDE.md` | Decidir con `/setup` si se quiere volcar el plan a Jira |
| ☐ 42 | CS-48 | Precio de la librería de producción (300 €/mes) marcado `⚠️ verificar` — se usa en las comparativas del brief | `evaluation.md` §6.5a | Confirmar la cifra real de la suscripción |
| ☑ 43 | CS-30 | ~~Discrepancia declarativa: R-18 exige reservar las 116 h «antes de arrancar la Fase 0» pero ninguna tarea lo exige como dependencia~~ — **cerrado 2026-09-01** junto con el ítem 21: las 116 h son **N/A en modo personal** (`gates/gobernanza.md` §6.2) | `evaluation.md` R-18 vs `tasks.md` | ✅ Cerrado (ver ítem 21) |
| ☐ 44 | CS-33/CS-32/CS-37 | Entregables de F2 aún no escritos (`Dockerfile` de T-05, `concurrency_profile.py` de T-04, `ace_step_shim.py` de T-03) — **trabajo del ledger, no precondición**; listados aquí solo para visibilidad. **🆕 2026-09-02: dos de tres escritos.** `Dockerfile` ✅ (imagen `ace-step-runner:t05`, base por digest con aserciones de build sobre Pascal) y `ace_step_shim.py` ✅ (con él se cerró `T-05` entero). **`concurrency_profile.py` sigue sin existir y es deliberado**: para la mitad local de `T-04` no hacía falta código — sumar dos perfiles de VRAM es aritmética, y forzar dos inferencias en 8 GB tumba el demonio de Docker. Se escribe **cuando haya GPU de ≥ 24 GB**. Aparecieron por el camino, sin estar previstos: `carga_contigua.py`, `text_conditioning.py`, `gpu_tiers.py`, `tools/build_artifact.py` y siete sondas en `spikes/`. | `apps/runner/spikes/README.md` | Se resuelven ejecutando sus tareas |

---

## D. Cabos sueltos NUEVOS, descubiertos al ejecutar F2 (2026-09-02)

> Los cuatro salieron de tener el modelo corriendo, no de leer documentación. Ninguno bloquea el gate G1 ni cambia una cifra ratificada; **dos de ellos sí cambian lo que el protocolo de G1 debe decir antes de la primera escucha**, y otro cambia lo que se le puede prometer al usuario en la UI.

| # | ID | Cabo suelto | Fuente | Fecha límite natural | Dueño |
|---|----|-------------|--------|----------------------|-------|
| ☐ 45 | **CS-52** 🆕 | **Las etiquetas de sección no son cosmética: el formato cambia el resultado.** ACE-Step espera las **canónicas** (`[intro]`, `[verse]`, `[chorus]`, `[bridge]`, `[outro]`). Las de estilo Suno (`[VERSO 1 - HOMBRE — RAP, entra el beat]`) **van verbatim al modelo**: la letra se le entrega dentro de la plantilla `# Languages… # Lyric…` sin filtrar nada, así que una etiqueta que el modelo no reconoce es texto que compite con la letra. **A/B limpio del 2026-09-02** — misma semilla (20260902), mismo prompt de estilo, **mismo cuerpo de letra carácter a carácter** (2.838 caracteres tras retirar las etiquetas, comprobado programáticamente), **única diferencia las 9 etiquetas**: `out/libre-240s-…wav` (Suno) frente a `out/libre-canonica-240s-…wav` (canónicas). El propietario confirmó **de oído** que las canónicas suenan mejor. **⚠️ Discrepancia declarada, no disimulada:** circula la cifra de «+47 % de pulso» a favor de las canónicas y **no se reproduce**. Recalculado hoy con la propia herramienta del proyecto (`apps/runner/spikes/medir_ab.py`) sobre ese par exacto, el descriptor de pulso `acf_env_pico` **baja** de 0,7295 a 0,6453 (**−11,5 %**); lo que sube es brillo (`rolloff95` +35,6 %, energía > 4 kHz +45,8 %) y número de cambios de sección (31 → 39, +25,8 %). **La preferencia de oído no está en disputa; el número sí, y no debe citarse hasta reproducirlo.** **Dos cierres pendientes:** (a) `T-46` (editor de letras) debe **validar y normalizar** a las canónicas, no dejar pasar el formato Suno — su criterio actual dice «etiquetas válidas y parseables por la API» sin definir cuáles; (b) el protocolo de G1 debe **fijar qué formato usan los 10 briefs**, porque hoy no lo dice y si cada brief usa el suyo la sesión compara cosas distintas. | `out/libre-informe.json` vs `out/libre-canonica-informe.json` · `adapters/ace_step/vendor/pipeline/constants.py::formatear_letra` · `tasks.md` T-46 | **Antes de `T-09`** (formato de los briefs, dentro de la ratificación de `T-08`) y **antes de `T-46`** (validador) | Propietario (briefs) + quien ejecute `T-46` |
| ☐ 46 | **CS-53** 🆕 | **ACE-Step no permite repartir voces por sección — diferencia de capacidad frente a Suno.** No hay canal para decir «este verso lo canta él, el estribillo ambos, el final con coro». El único enganche de voz que existe es un **vector de timbre global de 2.048 dimensiones** que ocupa **1 de los 125 tokens** del condicionamiento y describe la pista **entera** (`matriz-capacidades.md` §3.4; además sobrevive casi igual a invertir el audio en el tiempo, o sea que captura carácter global, no identidad ni estructura). Un dúo hombre/mujer o un coro solo se pueden **sugerir** por el prompt de estilo, globalmente y sin garantía. Se descubrió porque el brief de prueba lo pedía explícitamente (`[VERSO 1 - HOMBRE — RAP]`, `[CORO - AMBOS]`, `[CORO FINAL - AMBOS Y CORO]`) y el modelo, sencillamente, no tiene por dónde recibirlo. **Tres consecuencias:** (a) **la UI no puede prometerlo** — afecta a las opciones de voz de `T-47` y a lo que `ui-design.md` insinúa; (b) **ningún brief de G1 puede pedir reparto de voces**, o el gate penalizaría al modelo por no hacer algo que nunca pudo hacer, y eso no es medir calidad; (c) es entrada de decisión para **C-03** (Fase 3) y refuerza que la Fase 4 (clonación) siga en no-go. | `spikes/matriz-capacidades.md` §3.4 · `out/libre-informe.json` (brief con reparto de voces) · `ui-design.md` · `tasks.md` T-47 | **Antes de `T-09`** (briefs de G1) y **antes de escribir la copy de `T-47`** | Propietario (alcance prometido) |
| ☐ 47 | **CS-54** 🆕 | **La matriz de `T-07` puede estar midiendo el techo del TURBO, no el de ACE-Step.** Todo se sondeó sobre `acestep-v15-turbo`, que es el checkpoint empaquetado. El Model Zoo de upstream marca **`Extract`, `Lego` y `Complete` como no soportadas en `turbo`** y **sí soportadas en `acestep-v15-base`** (verificado hoy en el README de la revisión fijada `19671f40…`). Por tanto **un negativo de la matriz puede ser propiedad del checkpoint y no del modelo**. Y funciona en las dos direcciones, que es lo inquietante: `CONTINUATION` salió **«sí» empíricamente en turbo** pese a que la tabla marca `Complete` ❌ — así que **ni la tabla predice bien ni la matriz es un techo**. **Lo que lo hace caro de ignorar:** `extract` («Extract {TRACK_NAME} track audio», con 12 nombres de pista) está en el vocabulario de tareas del modelo, declarado soportado en `base` y **bajo licencia MIT** — justo el agujero que dejó abierto el hallazgo de que los pesos de **Demucs son CC-BY-NC** (I-13b/CS-22), que hoy bloquea C-06. **Coste de comprobarlo:** `base` son **50 pasos con CFG** frente a los 8 sin CFG de turbo, es decir **6–12× más cómputo por pista** sobre una GTX 1070 — no es un experimento de Fase 0. Recomendación de los propios spikes: **no abrir spike de `base` ahora**; enganchar la prueba a la tarea que reabra C-06 y, mientras tanto, **declarar los negativos como «no soportado por la configuración de Fase 1»**, nunca como «ACE-Step no puede». | README de `ACE-Step/Ace-Step1.5` rev. `19671f40…` (Model Zoo) · `spikes/comparativa-modelos.md` §4.3 (acción A-5) · `spikes/matriz-capacidades.md` §1.4 y §6.5 | Antes de **estimar F11** (C-07/C-08) y antes de **reabrir C-06** | Quien planifique F11 / quien reabra C-06 |
| ☐ 48 | **CS-55** 🆕 | **El arranque en frío medido contradijo la promesa de 2–6 min que `ui-design.md` le hace al usuario — y la promesa sigue sin verificarse.** `ui-design.md` §3.3 fija «arranque en frío de 2–6 min si el pod está apagado (S-01, S-12)» con cronómetro honesto que cuenta hacia arriba («2:14 de ~2–6 min»). **Medido el 2026-09-02: `vram_load` de 700,7–721,6 s y arranque completo de 755,5–778,7 s — 12,6–13,0 min, entre 2 y 4× por encima del rango prometido**, y la promesa aún no estaba escrita en código de producto. Causa medida (no supuesta): lectura tensor a tensor sobre el bind mount, a 10,9–11,9 MiB/s. **Arreglado el mismo día** con lectura contigua: `vram_load` 72,9–83,3 s y arranque completo **109,7–140,2 s (1,8–2,3 min)**, reverificado hoy en `out/t05-health-informe.json`. **Por qué sigue abierto pese al arreglo:** eso es el **término de carga del modelo con imagen y pesos ya locales**. Lo que S-01 mide de verdad — *scheduling* del pod, *pull* de la imagen (~11,5 GB) y descarga de pesos (7,5 GB) — **no está medido y solo se puede medir en RunPod, hoy aparcado** (ítem 8). O sea: el rango 2–6 min es **plausible, no verificado**. Detalle que lo vuelve urgente y no teórico: **la copia ya está escrita en el código** — `adapter.health()` devuelve literalmente «*Rango esperado 2-6 min con imagen de contenedor cacheada, 5-12 min sin cachear (S-01)*» durante toda la carga. Un mensaje al usuario que hoy nadie ha comprobado. **Cierre:** o se mide S-01 contra RunPod (`T-03`), o se **reescribe el rango de `ui-design.md` y de `health()`** con lo único verificado — y en ese caso hay que decir de qué escenario se habla, porque el número local y el número cloud no son la misma cosa. | `ui-design.md` §3.3 (líneas 150 y 191) · `adapters/ace_step/adapter.py::health()` · `out/t05-health-informe.json` · `tasks.md` T-03 (criterio 3) | Antes de implementar la copy de espera (**`T-16`/`T-48`**) y antes de citar S-01 como hecho | Quien ejecute `T-03` (medición) + propietario (decidir qué se promete) |

---

## Resumen ejecutivo

- **🆕 2026-09-02 (cierre de F2) — F2 arrancó y está medio cerrada. Lo que quedó fuera es exactamente lo que cuesta dinero.** `T-05` (contenerización), `T-06` (comparativa) y `T-07` (matriz de capacidades) están **completadas con evidencia ejecutada**; el pipeline genera audio real de 240 s en castellano. `T-03` y `T-04` siguen `en-progreso` **por la misma causa única**: necesitan pod de RunPod y **RunPod está aparcado por presupuesto**. `T-08` sigue `en-revision` esperando **una firma que solo puede dar el propietario**. **CS-50 cerrado** (artefacto construido, y reconstruido con planificador); **CS-51 abierto solo por la firma**, no por el código — el arreglo lleva toda la jornada en uso.
- **🆕 Y ejecutar el spike destapó cuatro cabos sueltos nuevos (sección D, CS-52 a CS-55)**, dos de los cuales tocan el gate: **las etiquetas de sección cambian el resultado** con todo lo demás idéntico (CS-52) y **ACE-Step no reparte voces por sección** (CS-53), así que los 10 briefs de G1 no pueden pedir dúo ni coro y deben fijar un formato de etiquetas antes de generarse. Los otros dos son de honestidad técnica: **la matriz de `T-07` puede estar midiendo el techo del turbo y no el del modelo** (CS-54, con `extract` bajo MIT como candidato a desbloquear C-06) y **la promesa de arranque en frío de 2–6 min sigue sin verificarse** aunque el defecto que la rompía ya esté arreglado (CS-55, con esa copia ya escrita en `adapter.health()`).
- **⚠️ Regla que este cierre pone a prueba y conviene no relajar:** ninguna de las tres tareas cerradas hoy se cerró por «parece hecho». `T-05` exigió ejecutar una sonda nueva porque `health()` **nunca se había invocado** en una corrida real, y sin eso su criterio 3 habría quedado marcado sin evidencia. De `T-03` se marca **1 de 6** criterios, y es el más modesto (coste cloud cero).
- **🆕 2026-09-01 (tarde) — los 3 bloqueos originales de F2 están CERRADOS**: ítem 5 (GPU **GTX 1070, 8 GB**), ítem 6 (**toolkit verificado**, la GPU se ve dentro del contenedor) e ítem 7 (**pesos descargados y verificados**: 17/17 hashes correctos contra la revisión fijada `19671f40…`, 6,333 GB en `D:\srv\ace-step\`, manifiesto de procedencia escrito, pickle en cuarentena). La gobernanza (ítems 1–4) quedó cerrada por la adaptación a proyecto personal, y RunPod + tope de gasto (ítems 8–9) son opcionales, solo para el modo cloud.
- **🚨 *(superado el 2026-09-02, se conserva por trazabilidad)* Pero F2 NO puede arrancar todavía: dos bloqueos nuevos, ítems 7-bis (CS-50) y 7-ter (CS-51)**. (a) El **fichero único** `ace_step_1_5.safetensors` que el adapter exige **no existe publicado**: upstream son cuatro componentes y el *all-in-one* de terceros está descartado con tres motivos verificados (le falta el silence latent, no trae tokenizers, y son 9,337 GiB en una tarjeta de 8). Hay que **construirlo** — trabajo de `T-03`/`T-05`. (b) `_timing.py` fija `VRAM_FLOOR_MB = 8192` pero CUDA reporta **8191 MiB** en esta tarjeta, así que el propio código la declara **`NO VIABLE` por 1 MiB** y aborta antes de cargar nada. Exige **decisión sobre un umbral de la spec** (D-06/D-29), no un parche.
- **Aviso operativo de disco (matizado)**: durante la sesión `C:` llegó a bajar a **~5 GB libres (98 %)** y luego **se recuperó a ~57 GB (77 %)** — la caída fue **transitoria**, por el crecimiento momentáneo de VHDX dinámicos y del pagefile bajo carga (`docker_data.vhdx` 23,2 GB · `pagefile.sys` 10,8 GB · VM bundle 8,5 GB · WSL Ubuntu 4,5 GB), no por la descarga (que fue íntegra a `D:`). **No es un bloqueo**, pero sí un margen a vigilar: una imagen CUDA+torch son ~10 GB y el VHDX de Docker vive en `C:`. Recomendación (no urgente): mover el data-root de Docker a `D:` antes de construir la imagen de `T-05`. Los pesos, en cualquier caso, ya viven en `D:` y el `.gitignore` impide que entren al repo.
- **La regla de las mediciones**: `ACE_STEP_REQUIRE_GPU=1` en toda corrida real (guardarraíl G-01).
- **Hay una decisión económica pendiente** (ítem 25, CS-49): el instalador del runner GPU local (`T-86`/D-30) está **propuesto y estimado (+32 h / +1.920 €) pero no ratificado**. Automatizará los ítems 6–7 en cada máquina destino, pero **no los elimina hoy**: F2 arranca antes que F6, así que la verificación manual de la máquina de referencia (ítems 5–7) sigue siendo precondición del arranque.
- **Lo corporativo-legal no desaparece: espera en GC-01** (`gates/gobernanza.md` §8) — consulta legal real (I-05), I-05b, ToS de Suno, re-verificación de licencias del pipeline, Anexo A y gobernanza con personas reales, todo **antes de cualquier uso comercial o con terceros**.
- I-01 quedó cerrada el 2026-09-01 (equipo = el propietario en solitario): el calendario de 25–28 semanas es **orientativo por diseño**, no un compromiso pendiente de cerrar.

## Changelog

| Fecha | Cambio | Autor |
|---|---|---|
| 2026-09-01 | Creación: barrido exhaustivo de cabos sueltos sobre la planificación corregida (48 ítems + guardarraíl G-01). CS-43 cerrado el mismo día (precios reverificados). | revision-2026-09-01 |
| 2026-09-01 | **Adaptación a proyecto personal en solitario** — motivo: decisión del propietario: proyecto personal en solitario (posible comercialización futura). Sección A: ítems 1–4 (CS-05/06/07/08) **☑ cerrados** por la adaptación (ver `gates/gobernanza.md`; **GC-01 conserva la versión corporativa para el futuro comercial**); ítems 8–9 (CS-38/CS-39) pasan a **opcionales** (solo modo cloud; S-01/S-01b quedaría pendiente y el coste cloud de `evaluation.md` §6 como informativo); ítems 5–7 con dueño (propietario) — **únicos bloqueos reales de F2**. Sección B: CS-01/CS-02/CS-03/CS-04 (y la firma legal del manifiesto en CS-41) cambian su fecha límite al **gate de comercialización GC-01**; CS-46 ☑ (flujo solo: merge directo sin PR, `CLAUDE.md`); CS-10 ☑ (el propietario evalúa, sin agenda de terceros); CS-25/I-16 ☑ y CS-11+CS-27/I-18 ☑ (N/A en modo personal). Sección C: CS-42 ☑ (RACI: todos los papeles = propietario), CS-12/29/31 (I-01) ☑ (equipo = propietario en solitario, calendario orientativo), CS-09 ☑ (riesgo aceptado por escrito), CS-30 ☑ (junto al ítem 21). Cabecera y resumen ejecutivo actualizados. Ninguna cifra ratificada ni umbral de gate cambia (39.360 €, 7/10 ≥ 4/5, WER ≤ 15 %). | propietario (Daycry) |
| 2026-09-01 | **Ampliación propuesta del instalador GPU local (D-30/`T-86`).** (a) Ítems **6 (CS-35)** y **7 (CS-36)** anotados: pasan a estar **automatizados por el preflight de `T-86`** en cada máquina destino (con verificación SHA-256 de los pesos en CS-36), **pero la comprobación manual sigue siendo necesaria UNA vez** en la máquina de referencia de los spikes — `T-86` vive en F6 y F2 arranca antes, así que **CS-34 no desaparece** y ambos ítems siguen bloqueando F2. (b) Ítem nuevo **25 (CS-49)** en la sección B: **ratificar el delta de `T-86` (+32 h / +1.920 €) antes de ejecutarla**, con fecha límite natural antes de F6; los ítems de la sección C se renumeran 26–44. (c) Recuento de cabecera 48 → **49 ítems** y bullet nuevo en el resumen ejecutivo. Ninguna cifra ratificada cambia (39.360 €). | evaluator |
| 2026-09-01 | **Máquina objetivo identificada (ítem 5/CS-34):** el PC de desarrollo actual (Windows); despliegue final en esa máquina u otra (instalador `T-86`). Solo queda confirmar modelo de GPU y VRAM (`nvidia-smi`). I-22: Windows+WSL2 primer SO a soportar. | propietario (Daycry) |
| 2026-09-01 | **Ítem 5/CS-34 CERRADO: GPU confirmada — GTX 1070, 8 GB** (suelo de D-06/D-29, offloading obligatorio; Pascal → tiempos reales por medir en `T-03`, RunPod como alternativa). Ítem 6 concretado en 3 pasos: instalar driver NVIDIA (hoy ausente) → Docker Desktop/WSL2 → prueba `--gpus all`. Bloqueos restantes de F2: **ítems 6 y 7** (driver+toolkit y pesos). | propietario (Daycry) |
| 2026-09-01 | **Ítem 6, paso 1 hecho**: driver NVIDIA 582.66 instalado en la máquina de referencia (GTX 1070). Pendientes del ítem 6: Docker Desktop/WSL2 y la prueba `--gpus all`. | propietario (Daycry) |
| 2026-09-01 | **Ítem 7/CS-36 CERRADO con evidencia ejecutada** (`/dev-cycle`): descargados de `ACE-Step/Ace-Step1.5` **fijado a la revisión `19671f406d603126926c1b7e2adc169acbcade22`** los 17 ficheros del subconjunto mínimo viable (**6,333 GB**), **17/17 verificados** contra la atestación del publicador (0 fallos, 0 sin atestación), en `D:\srv\ace-step\upstream\<rev>\`; manifiesto de procedencia en `provenance/MANIFEST.json` (licencias **MIT** de ACE-Step + **Apache-2.0** del text encoder Qwen, `training_data_declaration` y advertencias); `silence_latent.pt` (pickle obligatorio) **en cuarentena fuera del volumen**; excluidos el LM 1.7B y las variantes XL con motivo registrado. La decisión de origen se tomó con fan-out de 4 análisis independientes + 3 jueces adversariales: el origen **sobrevivió los tres**; lo que refutaron fue el plan de post-proceso → ítems **7-bis** y **7-ter**. | orquestador `/dev-cycle` |
| 2026-09-01 | **Ítems nuevos 7-bis (CS-50) y 7-ter (CS-51)** en la sección A — los dos bloqueos que aparecieron al cerrar los tres originales: el fichero único de pesos **no existe publicado** (hay que construirlo; el *all-in-one* de terceros descartado con tres motivos verificados) y **`VRAM_FLOOR_MB = 8192` rechaza esta GTX 1070, que reporta 8191 MiB por CUDA**, abortando `load()` antes de tocar los pesos. Cabecera y resumen ejecutivo actualizados; añadido aviso operativo de disco (`C:` al 98 %). Ninguna cifra ratificada cambia (656 h / 39.360 €). | orquestador `/dev-cycle` |
| 2026-09-01 | **Ítem 6/CS-35 CERRADO con evidencia ejecutada** (`/dev-cycle`, Fase 0 de preparación): Docker Engine 29.7.2 con servidor `linux` sobre backend WSL2 (Ubuntu, v2) y `docker run --rm --gpus all nvidia/cuda:12.4.1-base-ubuntu22.04 nvidia-smi` **lista la GTX 1070 con 8192 MiB dentro del contenedor** (NVIDIA-SMI 580.159.06 / Driver 582.66 / CUDA 13.0). Cabecera y resumen ejecutivo actualizados: **el único bloqueo restante de F2 es el ítem 7 (CS-36, pesos)**. | orquestador `/dev-cycle` |

| 2026-09-02 | **Cierre de F2: el checklist se pone al día con lo ejecutado, y con lo que ejecutar destapó.** (a) Cabecera y resumen ejecutivo corregidos: el aviso «F2 NO puede arrancar todavía» queda **superado** — `T-05`, `T-06` y `T-07` están cerradas y el pipeline genera audio real; se conserva el texto anterior por trazabilidad. (b) **CS-50 cerrado**; **CS-51 sigue abierto solo por la firma** (el arreglo sostuvo toda la jornada de mediciones). (c) **CS-38/CS-39 actualizados**: RunPod **aparcado por presupuesto**, con las **tres** consecuencias contadas — S-01 en `T-03`, concurrencia L40S en `T-04` (y el ahorro de ~128 €/mes que no debe darse por ganado) y `T-29`/`T-34` (G1-bis), que el spike de `T-06` demostró que **no caben en GPU local**. (d) **Ítem 44 actualizado**: dos de tres entregables escritos; `concurrency_profile.py` sigue sin existir **deliberadamente**. (e) **Sección D nueva con CS-52…CS-55**; recuento 51 → **55 ítems**. **Ninguna cifra ratificada ni umbral de gate cambia** (656 h / 39.360 €; 7/10 ≥ 4/5; WER ≤ 15 %). | implementer (cierre de F2) |
| 2026-09-02 | **Nota metodológica sobre CS-52, dejada a propósito dentro del ítem.** Al registrar el hallazgo de las etiquetas de sección se intentó verificar la cifra que circulaba («+47 % de pulso a favor de las canónicas») recalculándola con la herramienta del propio proyecto sobre el par exacto de pistas: **no se reproduce** — el descriptor de pulso **baja** un 11,5 %; lo que sube es brillo y número de cambios de sección. El hallazgo cualitativo (las canónicas van y las de Suno estorban) **se mantiene**, con la preferencia de oído del propietario intacta; **la cifra queda marcada como no reproducida y no debe citarse**. Se registra aquí porque un número que nadie puede reproducir es exactamente lo que un checklist de cabos sueltos existe para atrapar. | implementer (cierre de F2) |

> 📋 **Registro de decisiones (2026-09-01).** Las decisiones abiertas de este checklist están resueltas o instruidas, una por una y con criterio, fundamento y firma asignada, en [`decisiones-2026-09-01.md`](./decisiones-2026-09-01.md) — 49 fichas, 34 pendientes de firma del propietario y 15 ejecutables. Ese documento **no sustituye** a este checklist: lo instruye.
